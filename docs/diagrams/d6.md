# Diagram 7 — Grafana Observability

```mermaid
flowchart LR
    DB[(InfluxDB 3)] --> G[Grafana]

    subgraph Dashboard_1["Dashboard 1 — Live Watch + HAR Monitoring"]
        W1[Watch accelerometer<br/>watch_imu_clean: acc_x acc_y acc_z]
        W2[Watch gyroscope<br/>watch_imu_clean: gyro_x gyro_y gyro_z]
        W3[Current HAR prediction<br/>real_har_predictions: predicted_label]
        W4[Prediction confidence<br/>real_har_predictions: confidence]
        W5[Prediction history table]
        W6[Stored watch rows / prediction counts]
    end

    subgraph Dashboard_2["Dashboard 2 — EEG/ECG Fake-Sensor Monitoring"]
        E1[EEG row count<br/>eeg_clean]
        E2[EEG signal preview<br/>selected EEG channels]
        C1[ECG row count<br/>ecg_clean]
        C2[ECG waveform<br/>ecg_value]
        V1[Sampling rate / latest rows]
    end

    G --> Dashboard_1
    G --> Dashboard_2